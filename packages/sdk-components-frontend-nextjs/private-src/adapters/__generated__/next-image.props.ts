import type { PropMeta } from "@webstudio-is/sdk";

export const props: Record<string, PropMeta> = {
  fill: {
    required: false,
    control: "boolean",
    type: "boolean",
    description: "",
  },
};
